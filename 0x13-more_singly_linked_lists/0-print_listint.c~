#include "lists.h"
/**
 *print_listint - this code shall print elemnt in lst
 *@h: this represent the head of lst
 *Return: it shall return the lst
 */
size_t print_listint(const listint_t *h)
{
  size_t x;
  for (x = 0; h; x++)
    {
      printf("%d\n", h->n);
      h = h->next;
    }
  return (x);
}
