#include <stdio.h>
/**
 *Description: main - this code prints A-Z two times
 * one in lower case and one in upper
 * Return: in sucess return 0
 */
int main(void)
{
char x;
char y;
for(x = 'a'; x <= 'z'; ++x)
{
putchar(x);
}
for(y = 'A'; y <= 'Z'; ++y)
{
putchar(y);
}
putchar('\n');
return (0);
}
