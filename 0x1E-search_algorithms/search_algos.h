#ifndef _SEARCH_ALGO
#define _SEARCH_ALGO

#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <math.h>

int linear_search(int *array, size_t size, int value);
int binary_search(int *array, size_t size, int value);

#endif
