#include "main.h"
/**
 *_strlen - this code prints the length of string
 *@s: this represent char s that will print the string len
 *Return: it shall return the length
 */
int _strlen(char *s)
{
int x;
for (x = 0; s[x] != '\0'; x++)
{

}
return (x);
}
